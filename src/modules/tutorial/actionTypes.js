import { APP_NAME } from 'config';

const MOD_PREFIX = `${APP_NAME}#TUTORIAL`;

export const SET_COLOR = `${MOD_PREFIX}/SET_COLOR`;
export const SET_FIGURE = `${MOD_PREFIX}/SET_FIGURE`;
export const CLEAN = `${MOD_PREFIX}/CLEAN`;
